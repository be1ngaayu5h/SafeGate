{
  "private": true,
  "name": "securacore-monorepo",
  "version": "1.0.0",
	"scripts": {
		"prepare": "husky install",
		"format": "pnpm --filter './client' format",
		"lint": "pnpm --filter './client' lint",
		"build": "pnpm --filter './client' build",
		"dev": "pnpm --filter ./client dev"
	},
	"devDependencies": {
		"eslint": "^8.0.0",
		"husky": "^9.1.7",
		"lint-staged": "^15.0.0",
		"prettier": "^3.5.3"
	}
}
